<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="techmarket.uno.fisherbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FisherBook">
        <activity
            android:name=".Logo_Activity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FisherBook.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Text_Content_Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>

<!-- заменить  местами лого и маин активити-->